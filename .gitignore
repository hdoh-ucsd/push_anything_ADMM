__pycache__/
*.pyc
venv/
.env
CLAUDE.md
AGENTS.md

# Editor configs
.vscode/
.idea/
.claude/

# Python build outputs
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Milestones
milestones/

# Generated run outputs. Previously tracked deletions remain visible until
# committed; these rules prevent new runs from repopulating source changes.
/results/
/audit_output/
/logs/
profiling/results/
# Render and profiler intermediates
*.prof
frames/

# Local Claude Code settings (developer-scoped hooks, permissions)
.claude/settings.local.json

# Per-run solver/tool leftovers that reappear on every launch.
gurobi.log
*.tmp.*

# Local upstream DAIRLab checkouts used for source comparison.
/external/
# Local diagnostic checkout copies are preserved separately from source.
/.diagnostic_worktrees/

# Local reference papers (not repo content).
/papers/
