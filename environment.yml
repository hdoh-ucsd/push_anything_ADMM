name: push_anything_admm
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - ffmpeg
  - pip:
      # Versions observed during the 2026-08-21 baseline audit. These pins
      # describe the audited environment; they are not yet a compatibility
      # guarantee for other platforms.
      - drake==1.51.1
      - matplotlib==3.10.8
      - numpy==2.2.6
      - osqp==1.1.1
      - pytest==9.0.3
      - PyYAML==6.0.3
      - scipy==1.15.3
