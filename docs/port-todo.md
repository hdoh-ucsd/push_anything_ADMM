# Port TODO — reference-conformance work not yet landed

Tracks reference mechanisms identified during T-push bug hunts that remain
unported. Ordered by expected impact on T-push success rate.

## Active native C++ gate: complete `open_table` after stable xArm descent

**Updated 2026-08-31 — elevated authority, transaction latching, physical
successor dwell, and watchdog liveness subgates pass; full task remains
BLOCKING.** The native
`oim_c++_anything` processes build, exchange state/trajectory/torque messages,
pass isolated Cartesian tracking checks, and complete the first Sampling-C3+
solve. The staged exact-T acquisition planner also latches one goal-facing
boundary sample and discrete descent waypoints. Differential-IK conditioning
now stabilizes the approximately 300 mm descent and enables a monotonic contact
handoff and sustained negative-y push. The ten-state contact model now plans T
yaw, rejects candidates outside a 5 mm predicted-x corridor, and has repeated
goal-directed physical rotation without violating that corridor. Full
successor selection, collision-free repositioning, clearance handoff, physical
re-contact, and a diagnostics-off three-face sequence have now been exercised.
Full translation-plus-yaw task success has not been demonstrated.

Next implementation and acceptance gates:

1. **DONE locally:** add a weak five-joint posture task whose target is
   generated by constrained Drake differential IK, preserving the source
   velocity-servo wrapper and existing effort/velocity limits.
2. **PASS:** deterministic no-contact regression from the acquired planar pose to
   object height, keeping x/y error within the configured approach tolerance.
   Record final joints, tip, capsule endpoints, and process exit codes.
3. **PASS:** run `align-y -> align-x -> descend -> engage` and require the same exact-T
   boundary sample to remain latched until engagement. Reject any T motion
   during free-space descent.
4. **PASS at reduced-model gate:** live Sampling-C3+ remained in contact for
   13,500 updates, moved the T 16.058 mm in negative y, and reduced planar
   goal error by 16.010 mm. The same rollout accumulated 8.678 mm positive-x
   drift and did not control yaw.
5. **PASS at initial rotational gate:** extend contact planning to T yaw and
   reject lateral drift while preserving contact. The final repeated physical
   gate reduced yaw error by 0.029237 rad, limited x drift to 0.835 mm, and
   rejected 6/8 finite samples outside the configured 5 mm corridor. Commit,
   dirty-tree state, command, config checksum, deterministic seed status,
   dependencies, and raw output location are recorded in the C++ run log.
6. **PASS at successor-transition gate:** retain the active physical face,
   select a different-face successor using progress/cost hysteresis, and execute
   `lift -> traverse -> descend -> engage -> contact`. The quarter-turn witness
   autonomously selects `crossbar_right`. A diagnostic physical run switched at
   update 86,011, limited reposition-induced T motion to 0.605 mm and
   0.000162 rad, handed off inside the 8 mm clearance band, and established
   exact successor contact at a 2.955 mm gap. The force/timing diagnostics
   default off; an unforced regression made no premature switch and retained
   0.028505 rad yaw-error reduction with 0.505 mm lateral drift.
7. **PASS at sustained autonomous multi-face gate:** with diagnostics disabled,
   autonomously execute `crossbar_top_left -> crossbar_right ->
   crossbar_top_left`, prove side contact from Drake contact points, and combine
   rotation with negative-y translation. The 530-second run emitted the
   acceptance receipt after 1,000 continuation steps with 2.009 mm negative-y
   progress, 0.05123 rad yaw progress, and 2.505 mm lateral error. Three
   physical contact episodes were observed; both successor points were near
   z=35 mm, not the z=59.585 mm top surface. Raw logs:
   `/tmp/oim-autonomous-multiface-gate-pass.EuokfT`.
8. **IN PROGRESS; repeated-cycle subgate PASS:** repeat accepted task-directed
   face cycles until translation and orientation meet the unchanged YAML
   tolerances simultaneously. The diagnostics-off 620-second run completed two
   separately baselined cycles, five physical Drake contact episodes, and four
   collision-free repositions. The two cycle receipts measured 2.004/2.003 mm
   negative-y progress, 0.01959/0.02146 rad yaw progress, and 1.234/2.610 mm
   lateral error. The 2,000-step watchdog now requires both unchanged progress
   components and reselects when either is deficient. Raw logs:
   `/tmp/oim-repeated-cycle-gate.x45A6Y`.
9. **PASS at resource-bounded OSC subgate:** the shared `FastOsqpSolver`
   transferred neither ownership nor cleanup responsibility for freshly
   allocated CSC value/index arrays, leaking three arrays on every 500 Hz OSC
   workspace rebuild. OSQP matrices now own those arrays, and solver teardown
   explicitly releases its final workspace, matrices, and settings. OSC RSS
   changed only 160 KiB over a diagnostics-off 360-second sample window and
   only 4 KiB over its final 120 seconds; the same run retained two physical
   contacts and two collision-free repositions. A new 1,000-rebuild solver
   regression passes. Raw logs: `/tmp/oim-osc-memory-gate-fixed.NEjmVi`;
   preserved negative diagnostic: `/tmp/oim-osc-memory-gate.0YPa0w`.
10. **PASS at productive corrective-dwell subgate:** the diagnostics-off,
   isolated-port run entered measured recovery at 2.004 mm x error, reached a
   best 0.523 mm, and completed the unchanged 1,000-step dwell at 0.527 mm.
   Measured reduction was 1.480 mm against a new derived 1 mm receipt threshold
   (half the existing conservative guard); YAML, solver, dynamics, gains, and
   terminal tolerances were unchanged. Drake independently recorded the
   physical contact from 168.712 to 181.406 s. Raw logs:
   `/tmp/oim-productive-corrective-inline-isolated.No1IiU`.
11. **PASS at authority/arbitration audit; terminal FAIL:** elevated
   repositioning now crosses the existing 1 mm shaft gate using the existing
   reposition posture gains and model-derived damping compensation inside the
   unchanged clamps. Successor selection is latched through traverse and the
   configured physical dwell precedes ordinary guard switching. A canonical
   run proved selection, collision receipt, handoff, and physical contact on
   the same `stem_bottom` face. Another accepted a multi-face cycle with
   70.559 mm negative-y progress, 0.921741 rad yaw progress, and 4.647 mm
   lateral error. Raw accepted-cycle evidence:
   `/tmp/oim-autonomous-sustained-recovery.WO0Gyp`.
12. **IMPLEMENTED; physical acceptance BLOCKING:** contact tracking now
   constrains the entire inclined pusher capsule, not only the tip, and
   corrective recovery rejects measured wrong-polarity x response. The
   contact command is swept at nine samples against both exact
   radius-expanded T boxes and the table, excluding only the distal activation
   band. Unsafe commands hold position and use the existing collision-free
   reposition path. Wrong-polarity response after the unchanged dwell and
   derived 1 mm threshold selects another corrective face. A diagnostics-off
   300-second replay emitted ten command/clearance rejections and ten passing
   repositions with zero top contacts, but it also established zero intended
   Drake force contacts; runtime wrong-polarity acceptance is therefore not
   claimed. Raw safety evidence:
   `/tmp/oim-contact-phase-clearance.YBWT6E`.
13. **PASS for first physical face; continuation BLOCKING:** the
   capsule-feasible five-DOF contact-posture policy commands OIM's stick axis
   vertical-down through descent/contact and permits cylindrical contact only
   on the selected finite T face. A deterministic witness rejects an inward
   shaft at the same loaded tip. A diagnostics-off replay established eight
   physical side-contact episodes (13,392 samples, 19.2452 N peak) at
   z=34.41--34.82 mm, ten productive measured lateral recoveries, 0.404557 rad
   yaw progress, and 2.03546 mm lateral drift. One unsafe later command was
   rejected and repositioned. Raw evidence:
   `/tmp/oim-vertical-contact-gate.XJQsNG`.
14. **PARTIAL PASS; terminal safety BLOCKING:** reachability-conditioned
   successor engagement, second physical corrective-face contact, and runtime
   wrong-polarity rejection now pass. Drake reported the `crossbar_right`
   force episode at 223.098 s. A later measured response grew from 2.07508 to
   4.19107 mm and was rejected after the unchanged 1,000-step dwell. A loaded
   tangent-only correction then proved 1.23255 mm recovery in 46 steps. The
   full autonomous
   terminal rollout with repeated productive recovery. Translation and
   orientation must meet the unchanged 0.05 m and 0.10 rad YAML tolerances
   simultaneously. The first proof recovery retains the configured 1,000-step
   dwell; later independent measured receipts resume immediately. A stalled
   recovery cannot suppress the configured progress watchdog. The latest
   diagnostics-off 700-second terminal negative ended with 0.644466 m
   translation error, 1.82289 rad orientation error, and 0.218209 m lateral
   drift; it also recorded top-surface contacts near z=59.55--59.60 mm. Raw
   negative: `/tmp/oim-autonomous-recovery-watchdog.Ie81or`. Later attempts
   exposed upper-perimeter contact at z=59.58--59.59 mm and a blocked traverse
   posture. The final fail-safe holds and replans instead of throwing and
   excludes the crossbar-top perimeter after initial acquisition, but still
   needs a physical terminal replay. Evidence:
   `/tmp/oim-successor-reachability.UMLJdz`,
   `/tmp/oim-full-gate-terminal.QgC4uD`, and
   `/tmp/oim-side-stem-successors.MJvnVy`. The first
   vertical-contact replay ended with 0.769181 m translation error and
   2.73703 rad orientation error because its `crossbar_right` successor
   stalled 8.975 mm short in engagement. Do not retune
   dynamics, solver settings, progress minima, or terminal tolerances to pass
   this gate. Only after terminal C++ success should this path be ported to
   Python.

Startup, first contact, incidental T displacement, or a completed 50,000-step
process run are not scenario success.

**Status audit 2026-07-22 (arc af44d06):** items #1, #2, #3, #4, #5,
#6 all landed on `main` (verified against HEAD). Item #7 (full
reference q_vector) is STRUCTURALLY BLOCKED on a G-matrix ADMM port
+ LCS state layout parity — see §7 for the empirical migration
receipts. See each section for the file:line evidence + landing
commit.

## 1. Parallel sample evaluation (LANDED — commit `af44d06`)

**Reference**: `sampling_based_c3_controller.cc:971`
`#pragma omp parallel for num_threads(num_threads_to_use_)`
with `num_outer_threads` resolved at :415-422.

**Port** (LANDED): `InnerSolver._lazy_init_worker_kits(n)` in
`control/sampling_c3/inner_solve.py` builds n per-worker kits, each
holding its own `plant_ctx`, `plant_ad` context, `LCSFormulator` (own
`_last_contact_info` cache), and `C3Solver` (own `_u_prev_solve`,
`_last_lambda_*`). `evaluate_samples` runs k=0 serially (keeps its
diagnostic stream) then dispatches k>=1 through a `ThreadPoolExecutor`
fed from a queue of kits. Stdout is globally suppressed around the
pool because `contextlib.redirect_stdout` swaps process-global
`sys.stdout` — per-worker suppression would race.

Config: `SamplingC3Params.num_threads_to_use: int = 1` (default serial;
`num_outer_threads` alias honoured on load). Env override
`PORT_NUM_THREADS_TO_USE` for A/B smoke tests without reloading yaml.

Push_t canonical validation (30 s):
- Serial baseline: avg_per_step_ms=105.0.
- Parallel-3: avg_per_step_ms=54.6 (1.92× speedup), no exceptions,
  sim completes. QP-failure rate ticks 0.02%→1.00% (OSC main-thread,
  attributable to ADMM ordering drift under parallel dispatch);
  trajectory nondeterminism within noise per the push_t plan doc.

Default remains serial; push_t yaml opt-in deferred pending a
coordinated regression sweep before promoting `num_threads_to_use`
above 1 as the default.

## 2. `use_predicted_x0_c3` (ALREADY PORTED)

**Reference**: `sampling_based_c3_controller.cc:1418-1450`
**Port**: `control/ci_mpc_c3plus.py:118-142` +
`control/ci_mpc_c3plus.py:326-345` (clamp block).

Marked here for cross-reference — no work needed. The clamp implementation
uses `x_seq[1]` as `_x_pred_curr_plan`, which is functionally equivalent to
reference's interpolation between `knots.col(last_passed_index)` and
`knots.col(last_passed_index + 1)` when `filtered_solve_time_ / dt ≈ 1`.

## 3. `BufferedSample` carrying full `SampleResult` (LANDED 2026-07-19)

**Reference**: `sample_costs_buffer_` matrix stores position + cost, but the
per-sample C3 plan (`c3_buffer_plan_`) is stored alongside.
**Port** (LANDED): `BufferedSample.result: Optional[object]` at
`control/sampling_c3/sample_buffer.py:56`; write site
`control/sampling_c3/sampling_based_c3_controller.py:1023-1031` populates
`result=r` from the per-sample SampleResult; `AugmentSamplesWithBuffer`
re-enabled at controller `:1417-1442`, gated by
`consider_best_buffer_sample_when_leaving_c3` (default True; set True
in `config/sampling_c3_kik_t.yaml`). Match to reference cc:2106-2158.

## 4. `kRandomOnPerimeter` sampler (LANDED — commit `fc582aa`)

**Reference** push_t uses `sampling_strategy: 4` (`kRandomOnPerimeter`).
**Port** (LANDED): implemented at `control/sampling_c3/sampling.py:657`;
`SamplingStrategy.kRandomOnPerimeter = 4` at `params.py:77`; push_t yaml
opts in via `sampling_strategy: kRandomOnPerimeter` at
`config/sampling_c3_kik_t.yaml:278`. Reference-parity `grid_x/y_limits`
window added at commit `fc582aa`.

**Historical probe 2026-07-19 (yaw_face_bias_strength)**: dropping
`yaw_face_bias_strength: 500 → 0` under the old `kFaceNormal` sampler
gave uniform 10/10 face coverage but killed c3 entries entirely. Landed
solution was to swap to `kRandomOnPerimeter` + tune cost pipeline
(commits `3d335f3`, `fc582aa`). Baseline evidence:
`push_t_show_20260719_022819.txt` (old bias) vs
`push_t_show_yawbias0_20260719_023523.txt` (probe).

## 5. `CheckForWorkspaceLimitViolations` (HARD-PORTED — commit `6563e37`)

**Reference**: DRAKE_DEMAND aborts on workspace-axis or radius-shell exit
(`sampling_based_c3_controller.cc:1476-1494`).
**Port** (LANDED): `SamplingParams.strict_workspace: bool = False` and
`SamplingParams.robot_radius_limits: [float, float] = [0.0, 100.0]` at
`control/sampling_c3/params.py`; controller raises RuntimeError on first
violation when `strict_workspace=True`. push_t canonical yaml opts in
(`strict_workspace: true`, `robot_radius_limits: [0.25, 0.75]`). Default
preserves existing soft-log behaviour for all other yaml callers.

## 6. `avoid_choosing_unsuccessful_samples` (LANDED)

**Reference**: `sampling_based_c3_controller.cc:2161-2205`. Tracks failed
samples (samples where c3 mode was entered but progress not made) in a
buffer keyed by object pose. When generating new samples, rejects any within
`unsuccessful_radius` of an active entry.

**Port** (LANDED): `UnsuccessfulSampleBuffer` at
`control/sampling_c3/sample_buffer.py:172-253`; `sample_avoids_bad_spots`
filter at controller `:501-521` (fires when
`avoid_choosing_unsuccessful_samples` is True — default True); append on
free→c3 transitions at controller `:1963` (mirrors reference cc:2177).
Matches reference generate_samples.cc:187-205 sample-avoids-bad-spots
contract.

## 7. Full reference `q_vector` Q construction (STRUCTURALLY BLOCKED)

**Status audit 2026-07-22 (arc af44d06):** structurally blocked, not a
simple `use_reference_q_vector: true` flip. Requires two paired
structural ports before the reference Q can converge on the port's
ADMM. Every surface-level attempt has diverged.

**Cost pipeline is already ported.** `task_costs.py:785-802`
build_ee_space assembles `Q_base = w_Q · diag(q_vector)` from
`q_vector_ee_pos / obj_quat / obj_pos / ee_vel / obj_ang_vel /
obj_lin_vel`, remapped from reference layout `[ee_pos, quat, obj_pos,
ee_vel, obj_ang_vel, obj_lin_vel]` into the port's `[quat, obj_pos,
ee_pos, obj_ang_vel, obj_lin_vel, ee_vel]`. Gated by
`use_reference_q_vector` (default false).

**Empirical migration attempts (all diverged, all 2026-07-22):**

| Attempt | Config | Outcome |
|---|---|---|
| p39/p40/p41 | Single-parameter migrations (obj_pos alone, obj_quat alone, etc.) | Each regressed or blew up in isolation |
| p44 | Full coordinated + admm_iter=3 (reference iter count) | trans=174.8 mm, T over-pushed |
| p46 | Full coordinated + admm_iter=25 (port iter count) | Dual explodes 1000×/solve |
| p68 | Full ref G matrix with `g_x=g_u=0` (reference values), q_vec off | Trans regressed |
| p69 | Full ref G matrix, q_vec on | T physically tipped over |

See `config/tasks.yaml:209-214` and `control/admm_solver.py:159-166`
for the receipts.

**Root cause (as noted in `config/tasks.yaml:213`):** "port scalar-ρ
ADMM vs ref per-slot G". Reference's ADMM applies `w_G · diag(g_vector)`
augmentation (`g_x=0, g_u=0, g_lambda=2, g_eta=1`) via per-slot ρ
scaling; port's ADMM applies uniform scalar ρ to all slots. Under the
reference's dense q_vector (which relies on the per-slot ρ to keep
velocity slots well-conditioned), the port's uniform ρ over-augments
state/input slots by ~100× and under-augments λ slots by ~50×, which
ill-conditions the whole system.

**Required for landing** (NOT attempted in this arc):

1. **Per-slot G-matrix ADMM.** `admm_solver.py:1057-1076` already has
   a `_use_g_matrix` code path gated by `REFCONF_USE_G_MATRIX=1`. Enabling
   it (attempts p68/p69) destabilises even without the q_vector change,
   suggesting the current implementation needs additional work
   (possibly per-iter ρ ramp, dt-dependent G tuning, or a different QP
   formulation).
2. **LCS state layout parity.** Reference LCS state order is
   `[ee_pos(3), quat(4), obj_pos(3), ee_vel(3), obj_ang_vel(3),
   obj_lin_vel(3)]`; port is `[quat(4), obj_pos(3), ee_pos(3),
   obj_ang_vel(3), obj_lin_vel(3), ee_vel(3)]`. Structural — touches
   `lcs_formulator.linearize_discrete_ee_space`, wrapper x0 construction,
   OSC state slots, viz plumbing. Multi-file, multi-day.
3. **Re-tune after both above land** — reference q_vector values were
   sized against reference velocity dynamics; may need adjustment for
   port's discretisation.

**Recommendation:** treat item #7 as a separate multi-arc project.
Not tractable in a single-session port push. When you're ready to
tackle it, start with a G-matrix debugging arc (make `_use_g_matrix=1`
non-destabilising in isolation) before touching q_vector at all.

**Deep investigation (2026-07-23, arc af44d06):** see
`docs/superpowers/investigations/2026-07-23-item7-deep-investigation.md`.
Key correction to prior framing: the LCS state layout is NOT the
actual blocker (port's Q remap is bijective). The real blocker is a
**three-way coupled configuration mismatch** — every single-flag
migration attempt (p39-p46, p68, p69) is a compensation for a
compensation, none stepping toward the reference regime.

Coordinated triple flip never attempted:
- `rho = 1.0` (was 100.0)
- `_use_g_matrix = True` (was False)
- `use_reference_q_vector = True` (was False)

Suggested next arc: ρ sweep in *current* config
(`_use_g_matrix=False`, `use_reference_q_vector=False`) — establishes
baseline ADMM behavior vs ρ before any coordinated flip.

---

## Not blocking, but worth noting

- **Reference uses `include_walls: false` for push_t**. Port doesn't have wall
  contact at all. Match.
- **Reference `nominal_ee_accel: 2` inherited from anything**. Port hardcodes
  2.0 in `ci_mpc_c3plus.py:__init__` (commit `d601f96`). Could be made
  yaml-loaded but not urgent.
- **Reference `lcs_dt_resolution: 4`** — LCS integrated at planning_dt/4.
  Port uses planning_dt directly. Would require refactoring
  `linearize_discrete_ee_space` to accept a sub-dt argument.
